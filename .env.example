# Database — Turso (LibSQL) hosted database
# Format: libsql://<db-name>-<org>.turso.io?authToken=<token>
DATABASE_URL=libsql://your-db-name.turso.io?authToken=your-auth-token

# JWT — generate a strong secret: openssl rand -base64 64
JWT_SECRET=replace-with-a-strong-random-secret
